{{- $releaseName := .Release.Name -}}
apiVersion: v1
kind: Secret
metadata:
  name: sftp-admin-credentials
type: Opaque
data:
  username: {{ .Values.secret.username | b64enc }}
  password: {{ .Values.secret.password | b64enc }}
