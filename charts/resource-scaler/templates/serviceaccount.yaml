{{- if .Values.serviceAccount.create }}
# ServiceAccount for cross-namespace scaling operations
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name }}
  namespace: {{ .Release.Namespace }}
{{- end }}
