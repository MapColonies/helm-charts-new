{{- if and .Values.alertmanager.enabled .Values.alertmanager.route.enabled -}}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: alertmanager-route
spec:
  {{- if .Values.alertmanager.route.host }}
  host: {{ .Values.alertmanager.route.host }}
  {{- end }}
  path: {{ .Values.alertmanager.route.path | default "/" }}
  to:
    kind: Service
    name: {{ .Values.alertmanager.fullnameOverride | quote }}
  {{- if .Values.alertmanager.route.tls.enabled }}
  tls:
    termination: {{ .Values.alertmanager.route.tls.termination | quote }}
    insecureEdgeTerminationPolicy: {{ .Values.alertmanager.route.tls.insecureEdgeTerminationPolicy | quote }}
    {{- if .Values.alertmanager.route.tls.useCerts }}
    certificate: {{ .Values.alertmanager.route.tls.certificate | quote }}
    key: {{ .Values.alertmanager.route.tls.key | quote }}
    caCertificate: {{ .Values.alertmanager.route.tls.caCertificate | quote }}
    {{- end }}
  {{- end -}}
{{- end -}}
